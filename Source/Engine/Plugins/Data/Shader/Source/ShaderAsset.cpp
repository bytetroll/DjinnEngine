#include <Data/ShaderAsset.h>
#include <Core/Common.h>

DJINN_NS2(Data);

ShaderAsset::ShaderAsset(Host::IRegistry *registry, COM::IUnknown *outer, COM::IInstanceHost* host) : IAsset(registry, outer, host) {

}

